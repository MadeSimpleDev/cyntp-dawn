{% schema %}
{
  "name": "USA Golf Courses",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    }
  ],
  "presets": [
    {
      "name": "USA Golf Courses"
    }
  ]
}
{% endschema %}

<section class="golf-courses-section color-{{ section.settings.color_scheme }}">
  <!-- 1) Gather All Courses from the "course" Metaobject Definition -->
  {% assign all_courses = shop.metaobjects.course.values %}

  <!-- 2) Build a Unique List of States -->
  {% assign states_csv = '' %}
  {% for c in all_courses %}
    {% if c.state and c.state.value != blank %}
      {% unless states_csv contains c.state.value %}
        {% assign states_csv = states_csv | append: c.state.value | append: ',' %}
      {% endunless %}
    {% endif %}
  {% endfor %}
  <!-- Split, remove duplicates, then sort alphabetically -->
  {% assign states_array = states_csv | split: ',' | uniq | sort %}

  <!-- 3) Section Header -->
  <div class="golf-courses-container">
    <div class="section-header">
      <h2 class="section-title">Golf Courses by State</h2>
    </div>

    <!-- 4) Loop Through Each State & Show Its Courses -->
    <div class="golf-courses-grid">
      {% for st in states_array %}
        {% assign st_stripped = st | strip %}
        {% if st_stripped != blank %}
          <div class="golf-course-state">
            <h3>{{ st_stripped }}</h3>
            <ul class="courses-list">
              {% for course in all_courses %}
                {% if course.state and course.state.value == st_stripped %}
                  <li>
                    <a
                      href="/pages/courses/{{ course.system.handle }}"
                      class="golf-course-link"
                    >
                      {{ course.name.value }}
                    </a>
                  </li>
                {% endif %}
              {% endfor %}
            </ul>
          </div>
        {% endif %}
      {% endfor %}
    </div>
  </div>

  <!-- (Optional) 5) Featured Destinations -->
  {% assign has_featured_destinations = false %}
  {% for course in all_courses %}
    {% if course.featured_destination == true %}
      {% assign has_featured_destinations = true %}
      {% break %}
    {% endif %}
  {% endfor %}

  {% if has_featured_destinations %}
    <div class="golf-courses-container">
      <div class="section-header">
        <h2 class="section-title">USA Featured Golf Destinations</h2>
      </div>
      <div class="golf-courses-grid">
        {% for st in states_array %}
          {% assign st_stripped = st | strip %}
          <!-- Only print states that contain a featured course -->
          {% assign has_featured_in_state = false %}
          {% for course in all_courses %}
            {% if course.featured_destination == true and course.state and course.state.value == st_stripped %}
              {% assign has_featured_in_state = true %}
              {% break %}
            {% endif %}
          {% endfor %}

          {% if has_featured_in_state %}
            <div class="golf-course-state">
              <h3>{{ st_stripped }}</h3>
              <ul class="courses-list">
                {% for course in all_courses %}
                  {% if course.featured_destination == true and course.state and course.state.value == st_stripped %}
                    <li>
                      <a
                        href="/pages/courses/{{ course.system.handle }}"
                        class="golf-course-link"
                      >
                        {{ course.name.value }}
                      </a>
                    </li>
                  {% endif %}
                {% endfor %}
              </ul>
            </div>
          {% endif %}
        {% endfor %}
      </div>
    </div>
  {% endif %}
</section>

<style>
  /* 🌟 Scoped Section Styling */
  .golf-courses-section {
    text-align: center;
    padding: 40px 20px;
    position: relative;
  }

  /* 🌟 Scoped Container */
  .golf-courses-section .golf-courses-container {
    margin-bottom: 40px;
    position: relative;
  }

  /* 🌟 Scoped Title Wrapper */
  .golf-courses-section .section-header {
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    /* Default to full width for mobile */
    max-width: 100%;
    margin-left: auto;
    margin-right: auto;
    width: 100%; /* Keep this for flex context */
    position: relative;
    padding: 10px 0;
  }

  /* Apply 70% width only on desktop */
  @media screen and (min-width: 750px) {
    .golf-courses-section .section-header {
      max-width: 70%;
    }
  }

  /* 🌟 Section Titles */
  .golf-courses-section .section-title {
    font-size: 22px;
    font-weight: bold;
    text-transform: uppercase;
    color: #c3996b !important; /* Golden Color */
    position: relative;
    background-color: var(--color-background);
    padding: 0 20px;
    z-index: 2;
    white-space: nowrap;
  }

  /* 🌟 Title Bars */
  .golf-courses-section .section-header::before,
  .golf-courses-section .section-header::after {
    content: '';
    flex-grow: 1;
    height: 2px;
    background-color: #c3996b !important;
    display: block;
  }
  .golf-courses-section .section-header::before {
    margin-right: 15px;
  }
  .golf-courses-section .section-header::after {
    margin-left: 15px;
  }

  /* 🌟 Grid Layout */
  .golf-courses-section .golf-courses-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 15px;
    max-width: 900px;
    margin: 0 auto;
    text-align: left;
  }

  /* 🌟 Each "State" Card */
  .golf-course-state {
    padding: 10px;
    background: transparent;
    border-radius: 4px;
    border: none;
  }

  /* Larger & Centered State Names */
  .golf-course-state h3 {
    font-size: 22px; /* Increase the size from 18px */
    margin-bottom: 8px;
    color: var(--color-heading, #333);
    text-align: center; /* Center the state name */
  }

  .courses-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .courses-list li {
    margin: 4px 0;
  }

  /* 🌟 Course Links */
  .golf-courses-section .golf-course-link {
    text-decoration: none;
    color: var(--color-text);
    font-size: 14px; /* Reduced from 16px */
    transition: color 0.2s ease-in-out;
  }
  .golf-courses-section .golf-course-link:hover {
    color: var(--color-accent);
    text-decoration: underline;
  }

  /* 🌟 Responsive Breakpoints */
  @media (max-width: 1024px) {
    .golf-courses-section .golf-courses-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  @media (max-width: 768px) {
    .golf-courses-section .golf-courses-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>