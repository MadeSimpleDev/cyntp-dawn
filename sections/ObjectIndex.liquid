{% schema %}
{
  "name": "Metafield Index",
  "presets": [
    {
      "name": "Metafield Index"
    }
  ]
}
{% endschema %}
{% paginate shop.metaobjects.course.values by 25 %}
  
  {% for metaobject in shop.metaobjects.course.values %}
    <li>
      {{ metaobject.name | link_to: metaobject.system.url }}
    </li>
  {% endfor %}

  {{ paginate | default_pagination }}
  {% endpaginate %}